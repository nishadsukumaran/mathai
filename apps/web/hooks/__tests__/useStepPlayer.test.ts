/**
 * useStepPlayer hook tests.
 *
 * The runnable tests live at:
 *   tests/unit/hooks/useStepPlayer.test.ts
 *
 * They are placed there to satisfy the root jest.config.ts testMatch pattern:
 *   "<rootDir>/tests/**\/*.test.ts"
 *
 * This file is a reference stub so the test can also be discovered relative
 * to the source file it covers.
 */

// Re-export from the canonical test location so tooling can find it.
export * from "../../../../tests/unit/hooks/useStepPlayer.test";
