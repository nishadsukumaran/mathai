/**
 * @deprecated — replaced by useProfile() hook.
 * Use `hooks/use-profile.ts` directly in view components.
 * Kept to avoid breaking any future imports that haven't been updated yet.
 *
 * @see hooks/use-profile.ts
 */
export { };
